<UserControl x:Class="NuGet.VisualStudio.PropertyPage.ProjectPropertyPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300" DataContext="{Binding RelativeSource={RelativeSource Self}, Path=ViewModel}">
    <StackPanel Orientation="Vertical">
        <TextBlock Text="THIS. IS. NuGet!" />
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Configuration:" />
            <TextBlock Text="{Binding ConfigurationNames}" />
        </StackPanel>
    </StackPanel>
</UserControl>
